# Engineering Decisions (changeable implementation choices)

These entries are **implementation defaults**, not previously approved product decisions. Product-level binding decisions live in the specification (sections 1–2) and are not restated here.

| # | Date | Decision | Rationale | Status |
|---|------|----------|-----------|--------|
| D-001 | 2026-09-16 | Working name **Herbal Evidence** (Hebrew UI: ראיות צמחים). Hebrew RTL first, structured for future localization (string tables, `dir` attributes, bidi isolation). | Spec §3 | default |
| D-002 | 2026-09-16 | Backend: Python 3.13, FastAPI, Uvicorn, `uv` for dependency management. | Spec §3; uv installed locally | default |
| D-003 | 2026-09-16 | Frontend: plain HTML/CSS/JS, no framework. Served by a static server (Caddy) in its own Railway service. | Spec §3 | default |
| D-004 | 2026-09-16 | Auth: Supabase Auth, email/password, email verification, password reset. Backend validates Supabase JWTs (iss/aud/exp). | Spec §3 | default |
| D-005 | 2026-09-16 | Admin manually assigns requests to researchers. Simple `assigned_researcher_id` column; replaceable. | Spec §3, open business decision | default |
| D-006 | 2026-09-16 | No request-update emails in v1. | Spec §3 | default |
| D-007 | 2026-09-16 | Separate Supabase projects `herbal-evidence-dev` and `herbal-evidence-prod`, in a **new Supabase organization** created for this product. | Spec §3, §12; user choice during planning | default |
| D-008 | 2026-09-16 | AI provider interface is replaceable; first concrete adapter targets **OpenAI**. Model and key via `AI_PROVIDER`, `AI_MODEL`, `AI_API_KEY`. Without a key the provider raises a structured "not configured" job failure — never mock content. | Spec §3; user choice during planning | default |
| D-009 | 2026-09-16 | Literature adapters: PubMed E-utilities (esearch/efetch) + Europe PMC REST. Optional `NCBI_API_KEY` for higher rate limits. | Spec §6 | default |
| D-010 | 2026-09-16 | Durable job queue in Postgres (`research_jobs`), claimed with `FOR UPDATE SKIP LOCKED`, lease + heartbeat, bounded retries, idempotency key; consumer runs inside the backend process lifespan. No Redis, no third service. | Spec §8 | default |
| D-011 | 2026-09-16 | Drafts and staff artifacts have **no** RLS policies for `anon`/`authenticated`; they are reachable only through the backend using the service-role key with explicit server-side authorization. | Spec §9–10 | default |
| D-012 | 2026-09-16 | Design created in Google Stitch (project `12444680124780591192`, design system asset `602809031577218080`). Screen IDs in `docs/design/stitch.md`. | Spec §11 | default |
| D-013 | 2026-09-16 | Work is executed in phases (0–6) with a review stop after each; progress logged in Obsidian `HERBAL_EVIDENCE_PROJECT/`. | User instruction | default |
| D-014 | 2026-09-16 | **Supersedes D-007.** Both Supabase projects live in the existing organization **`Gil AI Course`** (`bmwbpotofrnbijikkeib`) — "גיל AI קורסים" — not in a new organization. | User instruction, 2026-09-16 | active |
| D-015 | 2026-09-16 | **Supersedes D-002's `uv` choice.** `uv` is **not installed** on this machine; dependencies are declared in `backend/pyproject.toml` and installed with `pip`. Python 3.14 locally, `python:3.12-slim` in the image. Revisit if `uv` is installed later. | Verified with `Get-Command uv` | active |
| D-016 | 2026-09-16 | **Docker is not installed locally.** Dockerfiles are written for Railway's remote build and cannot be built or run here. Local verification uses a plain static server for the frontend and Uvicorn for the backend. This is a verification gap, recorded rather than papered over. | Verified with `Get-Command docker` | active |
| D-017 | 2026-09-16 | No local Supabase stack (Docker). Schema work runs against the remote `dev` project with `npx supabase db push --linked`; `supabase start` and `db diff` are unavailable. | Spec §12 adapted to the machine | active |
| D-018 | 2026-09-16 | **Supersedes D-013's Obsidian path.** Progress and product documentation live in the Obsidian vault folder **`Herbal Evidence/`** (`E:\Google Drive\ClaudeVo\Herbal Evidence`). `HERBAL_EVIDENCE_PROJECT/` was referenced by an earlier session but never created. | User instruction, 2026-09-16 | active |
| D-019 | 2026-09-16 | Repository lives at `E:\Google Drive\Claude Projects\קורס AI\plants app`, remote `github.com/go2gil-bot/herbal-evidence`. Moved there with full history from a sibling folder. | User instruction, 2026-09-16 | active |
| D-020 | 2026-09-16 | **Supersedes D-012.** The Stitch project in D-012 is not accessible from this account and cannot be cited as evidence of a design. Real project: `318546168212711602`; design system `assets/16b8291a31b74b9d8b5d37ec7bafa0b1` ("Clinical Evidence Review"). | Verified with `list_projects` / `get_project` | active |
| D-021 | 2026-09-16 | Stitch generated **1 of 10** screens; the rest timed out. `list_screens` returns empty even for existing screens, so a timed-out generation is unrecoverable. Remaining screens are designed directly in HTML/CSS against the recorded tokens rather than blocking on the MCP. | Observed over four generation calls | active |
| D-022 | 2026-09-16 | The generated mockup's "GRADE Low", "PRISMA", scanned-item counts, Cochrane Central and "share with your doctor" are **rejected** — they claim methodology and scope the product does not have. See `docs/design/stitch.md`. | Spec §5, §11 | active |
| D-023 | 2026-09-17 | Supabase `dev` project created: `herbal-evidence-dev`, ref `wnttlpxiycghqiibdpjr`, org `Gil AI Course`, eu-central-1. Free-plan orgs reject `--size`, so the flag is omitted. | Spec §12 | active |
| D-024 | 2026-09-17 | Client roles get **column-level** grants, not table-level. `requests.status` and `assigned_researcher_id` are never granted to `authenticated`; the generated column `user_visible_status` is. This makes the five user-facing statuses a database guarantee. | Spec §7, §10 | active |
| D-025 | 2026-09-17 | Deliberate refusals raise SQLSTATE `PT4xx`, which PostgREST maps to HTTP 4xx. `serialization_failure` (40001) must never be used: PostgREST retries it automatically, which turned the stale-draft refusal into a hang. | Observed 2026-09-17 | active |
| D-026 | 2026-09-17 | Auth tokens are ES256, verified against the project JWKS with issuer, audience and expiry checks. Role is read from `user_roles` server-side, never from a token claim. | Spec §10 | active |
| D-027 | 2026-09-17 | Reference seed (`seed.sql`) and mock seed (`seeds/dev_mock.sql`) are separate files. The mock seed aborts unless `app.allow_mock_seed = 'yes'` is set first, so it cannot be run against Production by habit. | Spec §10 | active |
| D-028 | 2026-09-17 | The browser talks to Supabase Auth directly (publishable key) but reaches data only through `/api/v1`. The backend verifies the token and applies authorization; the browser never holds a secret key and never queries PostgREST itself. | Spec §10 | active |
| D-029 | 2026-09-17 | Plant identification has exactly three outcomes: `resolved`, `ambiguous` (the user picks) and `not_found` (human review). An exact alias match beats a longer partial one; two plants sharing one exact alias is ambiguity. A chosen plant id is rejected unless it was one of the offered candidates. | Spec §4 | active |
| D-030 | 2026-09-17 | `Settings` resolves `.env` from the package location, not the working directory, so the service finds its own configuration regardless of how it is started. | Found while running uvicorn with `--app-dir` | active |
| D-031 | 2026-09-17 | Permitted status transitions live in one table (`app/domain/workflow.py`). **Nothing transitions into `published`** - publication is a side effect of approval, inside the approval transaction. Reopening a published request is admin-only. | Spec §7 | active |
| D-032 | 2026-09-17 | The draft hash is sha256 of canonically serialised JSON (sorted keys, no padding, UTF-8 as itself), so key order never invents a conflict and a single changed character always does. | Spec §7 | active |
| D-033 | 2026-09-17 | The job consumer runs in the FastAPI lifespan, one job at a time per process. Claim is a single atomic UPDATE with `for update skip locked`; an expired lease is reclaimable, which is what makes a job survive a restart. Capacity limit documented rather than hidden. | Spec §8 | active |
| D-034 | 2026-09-17 | AI-dependent job types raise `NotConfigured`, stored as `{"kind": "not_configured"}` on the job. They produce **no placeholder output** - a mock draft that reads like a finding is worse than no draft. | Spec §6, §15.5 | active |
| D-035 | 2026-09-17 | Frontend assets carry a `?v=` marker and Caddy sends `Cache-Control: no-cache` for html/js/css. Headers alone do not dislodge a copy a browser already cached, which cost real debugging time. `frontend/dev-server.py` mirrors this locally; `python -m http.server` does not and misleads. | Observed 2026-09-17 | active |
| D-036 | 2026-09-17 | Study labelling comes from **curated metadata** - MeSH headings and publication types from Europe PMC - not from words in the title. Silence returns `None`/`other` rather than a confident wrong label; `is_oncology = None` means "not indexed", which is different from "not oncology". | Spec §6.5 | active |
| D-037 | 2026-09-17 | All outbound research HTTP goes through `app/research/http.py`: two allowlisted hosts, https only, **redirects not followed**, bounded timeout and size, and a resolved-address check against private ranges. | Spec §10 SSRF | active |
| D-038 | 2026-09-17 | The search query carries only the herb and appetite terms. Weight and food intake are deliberately excluded - folding them in would turn a weight study into an appetite finding. Hebrew aliases are not sent to English indexes. | Spec §1, §5 | active |
| D-039 | 2026-09-17 | Every provider call writes its own `search_runs` row. The job succeeds only if at least one provider did; if all fail the job fails and the requester keeps waiting. A failed search never reads as "no evidence found". | Spec §6, §15.5 | active |
| D-040 | 2026-09-17 | Draft output is constrained twice: a strict JSON schema is **sent to the provider** so it does not invent a shape, and Pydantic validators reject what comes back - no numeric score, no "GRADE", no "PRISMA", certainty from a fixed word list, no claim citing a source that was not supplied, and no inference of safety from an absence of reports. A draft that fails validation is discarded, never saved for a researcher to fix. | Spec §5, §6.9 | active |
| D-041 | 2026-09-17 | `AI_MODEL` is set from the account's live catalogue via `python -m app.ai.models`, not from a remembered name. Currently `gpt-5.2`. | Verified 2026-09-17 | active |
| D-042 | 2026-09-17 | Only research material is sent to the AI provider: question, herb, preparation, population, treatment context and source metadata. No owner id, no email, no request id. | Spec §6 | active |
| D-043 | 2026-09-17 | Supabase `production` project created: `herbal-evidence-prod`, ref `sptckumvgpfgluyxxiug`, same org and region as dev. Migrations promoted explicitly; reference seed applied; **no mock data** - verified 0 requests and 0 sources. | Spec §12 | active |
| D-044 | 2026-09-17 | Service configuration is `railway.json` per service directory: Dockerfile builder, watch patterns scoped to that service, `/health` healthcheck on the backend, one replica. One replica is a capacity statement, not an oversight - the job consumer processes one job at a time per process. | Spec §13 | active |
| D-045 | 2026-09-17 | Every value in `frontend/.env.example` is public by construction, because `entrypoint.sh` writes them into `config.js` at container start. The file says so, so a secret placed there reads as a mistake rather than a choice. | Spec §10 | active |
| D-046 | 2026-09-17 | Railway project `herbal-evidence` (`176be415`). Services are created in one environment only, so `production` was **deleted and recreated as a fork of `dev`** - that is what gives both environments the same two service instances. The fork inherits the source branch, so production's triggers had to be moved from `dev` to `main` afterwards; forgetting that step would have had production deploying dev commits. | Observed 2026-09-17 | active |
| D-047 | 2026-09-17 | The GraphQL `Builder` enum has no `DOCKERFILE` value - Railway detects a Dockerfile under the service root directory. Build configuration therefore lives in `railway.json`, not in API calls. | Verified against the live schema | active |
| D-048 | 2026-09-17 | Railway injects `PORT=8080`; a generated domain defaults to the port it was created with. The backend domains were created targeting 8000 and returned 502 while the app was running fine on 8080. Domain target ports are now 8080 and must be checked whenever a domain is regenerated. | Observed 2026-09-17 | active |
| D-049 | 2026-09-17 | The idle worker backs off from 5s to 60s instead of polling at a fixed rate. Flat 5s polling is ~17k requests/day per environment against an empty queue; the cost of backing off is at most 55s of extra latency on the first job after a quiet spell, and no turnaround time is promised. | Observed in deploy logs | active |
| D-050 | 2026-09-17 | The acceptance scripts prefer the OS trust store via `truststore` when it is installed. This machine's bundled certifi roots are stale enough to reject a freshly issued Railway certificate that `curl` accepts. **Verification is never disabled** - turning it off would make every check in those scripts meaningless over TLS. | Observed 2026-09-17 | active |
| D-051 | 2026-09-17 | The pilot answer key lives in `pilot_answer_keys` with no client grants, not in the response body - a participant who can read the key is not being measured. Scores are computed at submission and stored, so editing a key later cannot rewrite what somebody was measured on. | Spec §14 | active |
| D-052 | 2026-09-17 | `dose_is_not_advice` has one correct answer in every review, so it is fixed in code rather than left to each answer key. A researcher key that contradicts it is rejected. | Spec §14 | active |
| D-053 | 2026-09-17 | Pilot results are reported for patients and caregivers separately, never pooled, and every response states that no sample size or success target has been agreed. Satisfaction is not measured at all. | Spec §14 | active |
| D-054 | 2026-09-17 | Both Supabase projects still had `auth.site_url` at Supabase's default `http://localhost:3000`, so every verification and recovery link pointed at the user's own machine. Set per environment, with an explicit redirect allow-list; production's list contains no loopback address. | Found by `config diff`, 2026-09-17 | active |
| D-055 | 2026-09-17 | Remote config is managed by `supabase/config.toml` + `scripts/auth-config.sh`, and only declared properties are pushed - the ten hosted properties the file does not mention (MFA, Twilio, pooler sizes, storage analytics) are left untouched. `config diff` is the review step, because a non-interactive push defaults to proceeding. | Spec §12 | active |
| D-056 | 2026-09-17 | The `[auth.email.smtp]` block stays **commented out** until real credentials exist. A declared-but-empty block would be pushed as empty and would break sending rather than leave it alone. | Reasoned from push semantics | active |
| D-057 | 2026-09-17 | SMTP provider: **Brevo** (free tier, no domain required). The SMTP block lives in `supabase/config.smtp.toml` and is appended by the script **only when all four variables are non-empty** - with one missing, the CLI passes `env(SMTP_HOST)` through as a literal string and still sets `enabled = true`, which would enable SMTP with nonsense and break sending. Verified both ways with `config diff`. | Gil's choice, 2026-09-17 | active |
| D-058 | 2026-09-17 | Without a domain, the sender is a free webmail address, which fails that provider's DMARC policy at the recipient. Acceptable for a small pilot where people expect the email; a domain is the real fix before wider use. Recorded so "emails are flaky" is not later mistaken for a bug in this code. | Reasoned, 2026-09-17 | active |
| D-059 | 2026-09-17 | SMTP credentials are per environment (`DEV_SMTP_*` / `PROD_SMTP_*`) with a separate Brevo key each. Every other secret here is already per environment; one shared SMTP key would have been the only one crossing the boundary, so a dev leak would have reached production sending. Verified with `config diff`: dev values present include the block, production stays untouched. | 2026-09-17 | active |
